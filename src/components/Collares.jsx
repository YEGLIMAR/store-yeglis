import Category from "../components/Category";
import { collares } from "../components/data";

const Collares = () => {
  return <Category title="Collares" products={collares} />;
};

export default Collares;
